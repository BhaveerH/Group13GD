using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShopVarHolder : MonoBehaviour
{
    public static bool bLevel1 = true, bLevel2 = false, bLevel3 = false, bLevel4 = false, bLevel5 = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
